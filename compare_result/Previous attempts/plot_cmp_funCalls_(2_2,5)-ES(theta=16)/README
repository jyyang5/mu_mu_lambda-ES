## Plot_cmp_funCalls_(2_2,5)-ES(theta=16)

Plot using the following setting.

	NUM_OF_RUNS = 500;
	sigma0 = 1;
	Length scale: theta = 16*sigma*sqrt(n)  
	(2/2,5)-ES 
	Evaluate and choose centroid as parent for each iteration