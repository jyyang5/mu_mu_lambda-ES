## Plot_cmp_funCalls_(3_3,10)-ES

Plot using the following setting.

	NUM_OF_RUNS = 500;
	sigma0 = 1;
	Length scale: theta = 8*sigma*sqrt(n)  
	(3/3,10)-ES 
	Evaluate and choose centroid as parent for each iteration