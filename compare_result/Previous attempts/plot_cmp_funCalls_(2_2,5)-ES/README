## Plot_cmp_funCalls_(2_2,5)-ES

Plot using the following setting.

	NUM_OF_RUNS = 500;
	sigma0 = 1;
	Length scale: theta = 8*sigma*sqrt(n)  
	(2/2,5)-ES 
	Evaluate and choose centroid as parent for each itertation